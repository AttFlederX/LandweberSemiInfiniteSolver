cauchy_examples = [

    # Ex. #0
    # GleExample(
    #     V = lambda x, y: (1 / (2 * np.pi)) * sps.kv(0, 1 * np.sqrt(((x - 4) ** 2) + ((y - 0) ** 2))),
    #     a = lambda x, y: 1,
    #     f = lambda x, y: -1,

    #     area = TwoDimensionalArea(
    #         D   = lambda q, t: np.array([   q*np.cos(t),   q*np.sin(t)     ]),
    #         dD  = lambda q, t: np.array([   -q*np.sin(t),  q*np.cos(t)     ]),
    #         d2D = lambda q, t: np.array([   -q*np.cos(t),  -q*np.sin(t)    ]),

    #         qRange = [0.0, 1],
    #         tRange = [0.0, 2*np.pi]
    #     )
    # )
]
